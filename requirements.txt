# Runtime dependencies for the Streamlit app (inference only). Python 3.10-3.13.
# Tested with: tensorflow 2.21.0 (bundles keras 3.15), numpy 2.4, pillow 12.3, h5py 3.14, streamlit 1.64.
# TensorFlow pins compatible numpy / ml-dtypes / keras itself; do not pin those separately.
tensorflow>=2.21
numpy>=2.0
pillow>=11.0
h5py>=3.12          # legacy .h5 model loading
streamlit>=1.50
