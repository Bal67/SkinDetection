# Runtime dependencies for the Streamlit app (inference only). Python 3.10-3.12.
# Tested with: tensorflow 2.19.1 (bundles keras 3.15), numpy 2.1.3, pillow 12.3, h5py 3.16, streamlit 1.64.
# TensorFlow pins compatible numpy / ml-dtypes / keras itself; do not pin those separately.
tensorflow>=2.16,<2.20
numpy>=1.26
pillow>=10.0
h5py>=3.10          # legacy .h5 model loading
streamlit>=1.50
