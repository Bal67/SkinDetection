# Training / evaluation / tests. Tested with pandas 3.0.6, scikit-learn 1.9.1, matplotlib 3.11.2.
-r requirements.txt
pandas>=2.1
scikit-learn>=1.3
matplotlib>=3.7
requests>=2.31      # image download from dataset URLs
pytest>=8
# Optional: boto3 (only for `prepare_dataset.py --download s3`; credentials via the AWS default chain)
